---
const { src, alt, class: className, ...props } = Astro.props;
const resolvedSrc = typeof src === 'object' && src !== null ? src.src : src;
---
<img src={resolvedSrc} alt={alt || ""} class={className} {...props} />
